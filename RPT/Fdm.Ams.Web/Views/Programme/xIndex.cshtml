@model Fdm.Ams.ViewModels.ProgrammeViewModel
@{
    ViewBag.Title = "Programme";
}
<body>
    <div class="is-flex">
        <partial name="_AcademyAdminAside" />
        <div id="update-type-modal" class="modal" tabindex="-1" role="dialog">
            <div class="modal-dialog modal-dialog-update" role="document">
            </div>
        </div>


        <section id="main-content" class="container-fluid [ min-height--full ] content__container">
            <div>
                <div class="row">
                    <div class="col">
                        <h2>@ViewBag.Title</h2>
                    </div>
                    <div class="col">
                        <button type="button" data-toggle="modal" data-target="#create-programme-modal" class="btn btn-primary pull-right"><i class="fa fa-plus  [ icon-space--right ]"></i>Create New</button>
                    </div>
                </div>
            </div>

            @if (ViewBag.SuccessMessage != null)
            {
                <div class="alert alert-success text-center alert-dismissible fade show mt-3" role="alert">
              @*<div class="alert alert-success m-3 text-center alert-dismissible fade show m-3 mb-3 ml-0 mr-md-1" role="alert">*@
                    @ViewBag.SuccessMessage
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            }

            <div class="table-responsive mt-3">
                <table id="create-programme" class="table"></table>
            </div>

            <partial name="Create" />
        </section>
    </div>
</body>
<script src="~/js/AcademyAdmin/Programme.js" type="module"></script>

